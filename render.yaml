services:
  - type: web
    name: video-downloader-api
    runtime: node
    buildCommand: npm install && pip3 install yt-dlp
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
